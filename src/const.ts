export const CARD_PREFIX = "gseptik" as const;

export const CARD_NAME = `${CARD_PREFIX}-cistern-card` as const;

export const CARD_EDITOR_NAME = `${CARD_PREFIX}-cistern-card-editor` as const;

export const CARD_TILE = `${CARD_PREFIX}-tile-card` as const;

export const CARD_DIALOG = `${CARD_PREFIX}-dialogue` as const;
